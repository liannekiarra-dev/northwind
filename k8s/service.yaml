apiVersion: v1
kind: Service
metadata:
  name: northwind-delivery
  labels:
    app.kubernetes.io/name: northwind-delivery
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: northwind-delivery
  ports:
    - name: http
      port: 80
      targetPort: http
